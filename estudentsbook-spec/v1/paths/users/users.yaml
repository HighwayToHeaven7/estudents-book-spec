get:
  summary: Get list of new users in system
  tags:
    - user-query
  operationId: get_new_users
  responses:
    '200':
      description: List of new users
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../models/user/user.yaml#/User'
