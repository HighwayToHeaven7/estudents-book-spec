get:
  summary: Get list of grups with all students in
  tags:
    - students-query
  operationId: get_groups_with_students
  responses:
    '200':
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../../models/user/student/students-in-group.yaml#/GroupWithStudentsResponse'
